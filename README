Library catalog web implementation on activerecord + sinatra

gem install sinatra
gem install sinatra-activerecord
gem install sqlite3

To generate a new DB

rake db:migrate

To run the server

ruby app.rb

Currently hooks into the HackerSchool website to create and authenticate users.

Users can edit and create books on their own, check out and return books.
